{
  "name": "template-go-vite-talla-next",
  "private": true,
  "version": "0.0.0",
  "description": "Template: Go, TypeScript, Vite, and Tälla UI (unstable)",
  "type": "module",
  "scripts": {
    "dev": "conc -n serv,vite -c red,blue \"go run ./server\" vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "run-tests-esbuild src/test.ts"
  },
  "license": "MIT",
  "prettier": {},
  "devDependencies": {
    "concurrently": "^9.0.1",
    "vite": "5",
    "talla-ui": "next",
    "@talla-ui/web-handler": "next",
    "@talla-ui/test-handler": "next",
    "@talla-ui/run-tests-esbuild": "next"
  }
}
